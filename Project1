#include <iostream>
#include <fstream>
#include <stdio.h>
#include "data.txt"
using namespace std;


struct Person
{
char Name[20];
float balance;
};

void Display(int PersonP[],int n)
{
  Person ok;
  std::ifstream hello;
  hello.open("data.txt");
  
  for(int i =0; i<n; i++)
  {
  std::cout<<PersonP[i]<< ok.Name <<std::endl;
  std::cout<< PersonP[i]<<ok.balance<<std::endl;
  }
  hello.close();
 
 /*  
   if(hello.is_open())
   {
    while( hello >> ok.Name>>ok.balance)
    {
      std::cout<<ok.Name<<""<<ok.balance;
    }
*/    
}

void FindRichest(int name, int amount)
{
  
  
}


int main()
{
  std::ifstream hello;
  hello.open("data.txt");
  Person ok;
  int entry = 0;
  
  while(!hello.eof())
      {
        hello>>ok.Name>>ok.balance;
        hello.getline(ok.Name, ok.balance);
       entry++;
      }
  hello.close();
  int n = entry;

 
  int PersonP[n];
  

  Display(PersonP,n);
   // FindRichest(a,n);
}

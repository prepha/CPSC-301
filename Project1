#include <iostream>
#include <fstream>
#include <stdio.h>
#include "data.txt"
using namespace std;


struct Person
{
char Name[20];
float balance;

};

void count()
{
  //int k =0;
  std::ifstream hello;
  hello.open("data.txt");
  
  
  Person ok;
  
   if(hello.is_open())
   {
    while( hello >> ok.Name>>ok.balance)
    {
      hello.getline(ok.Name, ok.balance);
    }
     hello.close();
   }
   // return k;
/*   // while it does not hit end of file, contiunely get line
    while(!hello.eof())
      {
        hello.getline(ok.Name, ok.balance);
      }
*/
}

void Display(int PersonP[],int n)
{
  Person ok;
  std::ifstream hello;
  hello.open("data.txt");
  
   if(hello.is_open())
   {
    while( hello >> ok.Name>>ok.balance)
    {
      std::cout<<ok.Name<<""<<ok.balance;
    }
     hello.close();
   }
   
}
   


void FindRichest(int name, int amount)
{

}


int main()
{
  std::ifstream hello;
  hello.open("data.txt");
  Person ok;
  int entry = 0;
  
  while(!hello.eof())
      {
        hello>>ok.Name>>ok.balance;
        hello.getline(ok.Name, ok.balance);
       entry++;
      }
  hello.close();
  int n = entry;


  int PersonP[n];
  
   
   
  count();
  Display(PersonP,n);
   // FindRichest(a,n);
}
